{
  "name": "server",
  "scripts": {
    "build": "cargo build --release ",
    "build:prod": "npm run build -- --target=$TARGET",
    "dev": "cargo watch -x run"
  },
  "devDependencies": {
    "ui": "workspace:*"
  }
}
